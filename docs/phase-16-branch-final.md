Phase 16 branch finalization notes.

The branch now contains the remediation implementation and its focused tests. CI is the source of truth for compile/test/build verification; the PR and post-merge develop run are required before closure.
